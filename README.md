# flask

### requirements.txt 설치
   ```bash
   pip install -r requirements.txt
   ```

### flask 실행
   ```bash
   python app.py
   ```
실행할 때 gpt_sentiment_summary.py의 OPENAI_API_KEY = os.getenv("OPENAI_API_KEY") 부분 수정 필요